# @inventory/cave-pilot-configs

Exports `buildResaurceHrPilotTomeConfig` and `buildSaurceWalletHoldPilotTomeConfig` for log-view-machine `createTome` registration.

**Dependency:** `log-view-machine` (file link in this monorepo layout).

**Consumers:** `log-view-machine/mod/node-mod-editor` and any host that registers the same pilots.
